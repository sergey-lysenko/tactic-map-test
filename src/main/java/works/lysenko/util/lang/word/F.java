package works.lysenko.util.lang.word;

import static works.lysenko.util.chrs.__.*;
import static works.lysenko.util.chrs.___.ING;
import static works.lysenko.util.chrs.____.*;
import static works.lysenko.util.data.strs.Swap.s;
import static works.lysenko.util.lang.word.S.SCREEN;
import static works.lysenko.util.spec.Symbols.*;

@SuppressWarnings({"ClassWithoutLogger", "MissingJavadoc", "StaticMethodOnlyUsedInOneClass", "AutoBoxing", "unused"})
public record F() {

    public static final String FAILED = s(FAIL, ED);
    public static final String FAILING = s(FAIL, ING);
    public static final String FAILURE = s(FAIL, U, RE);
    public static final String FAILURES = s(FAILURE, S);
    public static final String FALSE = s(FA, L, SE);
    public static final String FENCE = s(FE, N, CE);
    public static final String FENCES = s(FE, NC, ES);
    public static final String FILTER = s(FI, LT, ER);
    public static final String FINAL = s(F, IN, AL);
    public static final String FINDING = s(FIND, ING);
    public static final String FINGER = s(FING, ER);
    public static final String FIXED = s(FI, X, ED);
    public static final String FORWARD = s(FO, RW, A, RD);
    public static final String FOUND = s(FO, U, ND);
    public static final String FRACTION = s(FR, AC, TION);
    public static final String FRAME = s(FR, A, ME);
    public static final String FULLSCREEN = s(FULL, SCREEN);
    public static final String FULLY = s(FU, L, LY);
}
