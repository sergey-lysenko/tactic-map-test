package works.lysenko.util.lang.word;

import static works.lysenko.util.chrs.__.RO;
import static works.lysenko.util.chrs.__.ZE;
import static works.lysenko.util.data.strs.Swap.s;

@SuppressWarnings({"ClassWithoutLogger", "MissingJavadoc", "StaticMethodOnlyUsedInOneClass"})
public record Z() {

    public static final String ZERO = s(ZE, RO);
}
