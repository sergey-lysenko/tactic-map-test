package works.lysenko.util.lang.word;

import static works.lysenko.util.chrs.__.*;
import static works.lysenko.util.chrs.___.*;
import static works.lysenko.util.chrs.____.*;
import static works.lysenko.util.data.strs.Swap.s;
import static works.lysenko.util.spec.Symbols.*;

@SuppressWarnings({"ClassWithoutLogger", "MissingJavadoc", "StaticMethodOnlyUsedInOneClass", "AutoBoxing", "unused"})
public record W() {

    public static final String WAITING = s(WAIT, ING);
    public static final String WARNING = s(WARN, ING);
    public static final String WEIGHT = s(WE, IGHT);
    public static final String WEIGHTS = s(WEIGHT, S);
    public static final String WHETHER = s(WH, ET, HER);
    public static final String WHICH = s(WH, ICH);
    public static final String WHILE = s(WH, I, LE);
    public static final String WIDER = s(WI, D, ER);
    public static final String WIDGET = s(WI, DG, ET);
    public static final String WIDTH = s(W, ID, TH);
    public static final String WINDOW = s(WI, ND, OW);
    public static final String WITHIN = s(WI, TH, IN);
    public static final String WITHOUT = s(WITH, OUT);
    public static final String WORKAROUND = s(WO, RK, AR, OUND);
    public static final String WRAPPER = s(WR, AP, PER);
    public static final String WRITE = s(WRIT, E);
    public static final String WRITER = s(WRIT, ER);
    public static final String WRITING = s(WRIT, I, NG);
    public static final String WRONG = s(WR, O, NG);
}
