package works.lysenko.util.lang.word;

import static works.lysenko.util.chrs.__.*;
import static works.lysenko.util.chrs.___.*;
import static works.lysenko.util.chrs.____.*;
import static works.lysenko.util.data.strs.Swap.s;
import static works.lysenko.util.lang.word.D.DEFINED;
import static works.lysenko.util.lang.word.E.EXPECTED;
import static works.lysenko.util.lang.word.K.KNOWN;
import static works.lysenko.util.spec.Symbols.M;
import static works.lysenko.util.spec.Symbols.S;
import static works.lysenko.util.spec.Symbols.T;

@SuppressWarnings({"ClassWithoutLogger", "MissingJavadoc", "StaticMethodOnlyUsedInOneClass", "AutoBoxing"})
public record U() {

    public static final String ULTIMATE = s(UL, TI, MA, TE);
    public static final String UNABLE = s(UN, ABLE);
    public static final String UNCAUGHT = s(UN, CA, UGHT);
    public static final String UNCHANGED = s(UN, CHAN, GED);
    public static final String UNDEFINED = s(UN, DEFINED);
    public static final String UNEXPECTED = s(UN, EXPECTED);
    public static final String UNKNOWN = s(UN, KNOWN);
    public static final String UNMET = s(UN, M, ET);
    public static final String UNNAMED = s(UN, NA, M, ED);
    public static final String UNREACHABLE = s(UN, RE, AC, HA, BLE);
    public static final String UNRESOLVED = s(UN, RESO, LV, ED);
    public static final String UNTIL = s(UN, T, IL);
    public static final String UPDATE = s(UPDA, TE);
    public static final String UPLOADING = s(UP, LOAD, ING);
    public static final String UPPER = s(UP, PER);
    public static final String UPSTREAM = s(UP, STRE, AM);
    public static final String USERS = s(USER, S);
    public static final String USING = s(US, ING);
    public static final String UPDATING = s(UPDA, TING);
}
