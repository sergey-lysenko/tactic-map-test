package works.lysenko.util.lang.word;

import static works.lysenko.util.chrs.__.*;
import static works.lysenko.util.chrs.___.GRA;
import static works.lysenko.util.chrs.___.ING;
import static works.lysenko.util.data.strs.Swap.s;
import static works.lysenko.util.spec.Symbols.*;

@SuppressWarnings({"ClassWithoutLogger", "MissingJavadoc", "StaticMethodOnlyUsedInOneClass", "AutoBoxing"})
public record G() {

    public static final String GETTING = s(GE, TT, ING);
    public static final String GIVEN = s(GI, V, EN);
    public static final String GRAPH = s(GRA, PH);
    public static final String GREEN = s(GR, E, EN);
    public static final String GRIDS = s(GR, I, DS);
    public static final String GROUND = s(GR, OU, ND);
    public static final String GROUP = s(GR, O, UP);
}
