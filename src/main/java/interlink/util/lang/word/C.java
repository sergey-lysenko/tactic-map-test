package interlink.util.lang.word;

import static works.lysenko.util.chrs.__.AS;
import static works.lysenko.util.chrs.____.COMP;
import static works.lysenko.util.data.strs.Swap.s;
import static works.lysenko.util.spec.Symbols.S;

@SuppressWarnings({"StaticMethodOnlyUsedInOneClass", "MissingJavadoc"})
public record C() {

    public static final String COMPASS = s(COMP, AS, S);
}
